---
phase: 05-newspaper-render
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - run_all.sh
autonomous: true
requirements: [REND-01]

must_haves:
  truths:
    - "run_all.sh accepts --date YYYY-MM-DD (required) and --dry-run (optional)"
    - "Each step runs in order: step1_3.py → step4.py → step6.py → step7.py → step8.py"
    - "Any step failure stops the pipeline immediately"
    - "run_all.sh --date 2026-05-14 completes all 5 steps end-to-end"
  artifacts:
    - path: "run_all.sh"
      provides: "Full pipeline orchestration"
      contains: "step8.py"
  key_links:
    - from: "run_all.sh"
      to: "step8.py"
      via: "last step in pipeline"
      pattern: "step8.py"
---

<objective>
Create run_all.sh: a bash script that chains the entire news pipeline from raw collection through newspaper PNG rendering, with --date parameter forwarding and fail-fast error handling (per D-06).

Purpose: Single command to run the complete 5-step pipeline, making the project usable as a daily cron job or manual one-liner.

Output: `/mnt/e/Daily/run_all.sh` — ~40 lines, executable
</objective>

<execution_context>
@/mnt/e/Daily/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Daily/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-newspaper-render/5-CONTEXT.md
@.planning/phases/05-newspaper-render/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Pipeline step scripts and their CLI interface -->

All steps follow the same CLI pattern:
```
python3 {step}.py --date YYYY-MM-DD [--dry-run]
```

Steps in order (from D-06):
1. step1_3.py — Collection + HTTP validation
2. step4.py — Classification + filtering
3. step6.py — Body extraction (uses chromium)
4. step7.py — Summary generation (uses MiniMax API, needs .env)
5. step8.py — Newspaper rendering (HTML + PNG)

Data flows through: /mnt/e/每日新中国/{date}/ as numbered markdown files.
All steps exit 0 on success, non-zero on failure.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create run_all.sh — full pipeline orchestration script</name>
  <files>run_all.sh</files>
  <read_first>
    /mnt/e/Daily/step7.py
    /mnt/e/Daily/step8.py
  </read_first>
  <acceptance_criteria>
    - run_all.sh has `#!/usr/bin/env bash` shebang on line 1
    - run_all.sh has `set -euo pipefail` on line 3
    - `bash run_all.sh` without --date prints error and exits non-zero
    - `bash -n run_all.sh` exits 0 (valid bash syntax)
    - Script contains all 5 steps in order: step1_3.py, step4.py, step6.py, step7.py, step8.py
    - Each step invocation passes `$DATE` via --date and `$DRY_RUN` if set
    - Script uses `SCRIPT_DIR` to resolve step paths relative to its own location
    - `chmod +x run_all.sh` makes it directly executable
    - Each step failure prints error message with step name and exits 1
  </acceptance_criteria>
  <action>
Create `/mnt/e/Daily/run_all.sh` with the following structure:

1. Shebang: `#!/usr/bin/env bash`
2. Comment header: script name, usage line `./run_all.sh --date YYYY-MM-DD [--dry-run]`
3. `set -euo pipefail` for strict error handling (per Pitfall 5 in RESEARCH.md)
4. Argument parsing: parse `--date` (required, value in `$2`) and `--dry-run` (optional flag) from `$@`. If `--date` missing after parse, print "错误: --date YYYY-MM-DD 为必填参数" to stderr and exit 1.
5. Set `SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"` to resolve script directory
6. Define steps array in order: `("step1_3.py" "step4.py" "step6.py" "step7.py" "step8.py")`
7. Loop over steps: for each step, print "═══ 运行: {step} --date {DATE} {DRY_RUN} ═══", then run `python3 "$SCRIPT_DIR/$step" --date "$DATE" $DRY_RUN`. Check exit code immediately — if non-zero, print "❌ {step} 失败，停止执行" and exit 1.
8. After loop: print "✅ 全管道完成: $DATE"

Make the file executable with `chmod +x run_all.sh`.
  </action>
  <verify>
    <automated>cd /mnt/e/Daily && bash -n run_all.sh && chmod +x run_all.sh && bash run_all.sh 2>&1 | head -3; echo "exit: $?"</automated>
  </verify>
  <done>
    - run_all.sh exists, is executable, has valid bash syntax
    - Requires --date parameter, exits with error if missing
    - Chains all 5 steps with fail-fast behavior
    - Uses SCRIPT_DIR for portable path resolution
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → bash script | User-provided date string |
| bash → python3 subprocess | Controlled invocation via step scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | --date argument | mitigate | Python argparse validates date format (YYYY-MM-DD) in each step |
| T-05-05 | Denial of Service | pipeline step | mitigate | set -euo pipefail ensures immediate stop on failure |
</threat_model>

<verification>
1. `bash -n run_all.sh` passes syntax check
2. `run_all.sh` without args exits non-zero with error message
3. Script contains all 5 step references in correct order
4. `chmod +x run_all.sh` succeeds
</verification>

<success_criteria>
- run_all.sh chains the full pipeline: step1_3 → step4 → step6 → step7 → step8
- --date parameter forwarded to all steps
- Fail-fast on any step error (per D-06)
</success_criteria>

<output>
Create `.planning/phases/05-newspaper-render/05-02-SUMMARY.md` when done
</output>
