---
phase: 04-summary-gen
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - step7.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "load_dotenv() 显式指定 .env 路径，不依赖 CWD"
    - "API 调用失败时自动重试 1-2 次"
    - "fallback_summarize 过滤【纠错】/责任编辑等非正文噪音"
    - "连续 10-16 次 API 调用之间有间隔避免 429"
    - "body 超过 2000 字截断时打印提示"
  artifacts:
    - path: "step7.py"
      provides: "改进后的摘要生成脚本"
      contains: "load_dotenv(Path"
  key_links:
    - from: "step7.py::llm_summarize"
      to: "retry logic"
      via: "for attempt in range"
      pattern: "for.*attempt"
---

<objective>
对 step7.py 应用 RESEARCH.md 识别的 5 项非阻塞改进，提升生产健壮性。

Purpose: 虽然当前实现已满足所有需求（D-01~D-05），但这 5 项改进可显著降低生产运行中的失败率和信息丢失。
Output: 改进后的 step7.py（UAT 仍应全部通过）
</objective>

<execution_context>
@/mnt/e/Daily/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Daily/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-summary-gen/4-RESEARCH.md (Section "可改进但不阻塞")
@.planning/phases/04-summary-gen/4-CONTEXT.md
@step7.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 应用 5 项健壮性改进</name>
  <files>step7.py</files>
  <read_first>
    step7.py
    .planning/phases/04-summary-gen/4-RESEARCH.md (Section "可改进但不阻塞" 和 "Common Pitfalls")
  </read_first>
  <action>
    在 step7.py 中应用以下 5 项改进（按 RESEARCH.md §可改进但不阻塞 的编号）：

    **改进 1: load_dotenv() 显式路径**
    - 将 line 19 的 `load_dotenv()` 改为 `load_dotenv(Path(__file__).parent / '.env')`
    - 这样无论从哪个目录运行都能正确找到 .env 文件
    - 保持 import 结构不变（Path 已导入）

    **改进 2: API 重试机制**
    - 在 llm_summarize() 函数中，给 API 调用添加 1 次重试
    - 当 client.chat.completions.create 抛出异常时，sleep 2 秒后重试一次
    - 仅重试 1 次，避免长时间阻塞
    - 第二次仍失败则打印失败信息并返回 None
    - 在 try/except 中包裹重试循环

    **改进 3: fallback_summarize 过滤噪音**
    - 在 fallback_summarize() 的 body 处理之前，添加噪音行过滤
    - 移除包含以下关键词的行：【纠错】、【责任编辑】、责任编辑、记者、编辑、来源、免责声明
    - 使用 re.sub 或字符串替换，在 sents = re.findall(...) 之前处理 body
    - 参考原始 step7_summarize.py 的噪音过滤模式

    **改进 4: API 调用间隔**
    - 在 run() 函数的 for a in matched 循环中，每次 llm_summarize() 调用后添加 0.5 秒间隔
    - 仅在 API 调用成功（非 fallback）时才 sleep
    - import time 模块（添加到文件顶部 import 区）

    **改进 5: 截断提示**
    - 在 llm_summarize() 中 body[:2000] 截断前，检查 len(body) > 2000
    - 如果超过 2000 字，打印 "  ⚠ 正文超长截断: {title[:30]}... ({len(body)}字 → 2000字)"
    - 保持截断逻辑不变，仅添加提示

    **实施注意事项：**
    - 不要改变函数签名（parse_args, parse_1news, parse_2news, llm_summarize, fallback_summarize, run, main）
    - 不要改变输出格式（3新闻_概述.md 结构必须完全不变）
    - 不要改变 CLI 参数行为（--date, --dry-run）
    - 改进后总行数应约 240-260 行
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
from pathlib import Path

src = Path('step7.py').read_text()

# 1. load_dotenv explicit path
assert 'Path(__file__)' in src and 'load_dotenv' in src, 'Improvement 1: load_dotenv not using explicit path'
# Check it's NOT just bare load_dotenv()
import re
# Should have load_dotenv(Path(__file__)...) form
assert re.search(r'load_dotenv\s*\(\s*Path', src), 'load_dotenv() not using Path argument'

# 2. Retry mechanism
assert 'retry' in src.lower() or 'attempt' in src.lower() or src.count('chat.completions.create') >= 1, 'Improvement 2: no retry logic found'

# 3. Noise filtering in fallback
assert '纠错' in src or '责任编辑' in src, 'Improvement 3: no noise filtering in fallback'

# 4. API interval
assert 'time' in src and 'sleep' in src, 'Improvement 4: no time.sleep import/usage'

# 5. Truncation warning
assert '截断' in src, 'Improvement 5: no truncation warning'

# Syntax check
ast.parse(src)
print('All 5 improvements verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - load_dotenv 使用 Path(__file__).parent / '.env' 显式路径
    - llm_summarize 包含 1 次重试逻辑
    - fallback_summarize 在分句前过滤【纠错】/责任编辑等噪音
    - API 调用间隔 0.5 秒（time.sleep）
    - body 超 2000 字时打印截断提示
    - `python3 -c "import py_compile; py_compile.compile('step7.py', doraise=True)"` 通过
    - `python3 step7.py --date 2026-05-16 --dry-run` 仍正常运行（语法+逻辑不破坏）
  </acceptance_criteria>
  <done>step7.py 包含全部 5 项健壮性改进，语法正确，dry-run 正常</done>
</task>

<task type="auto">
  <name>Task 2: 验证改进后 UAT 仍全通过</name>
  <files>.planning/phases/04-summary-gen/4-UAT.md</files>
  <read_first>
    step7.py
    .planning/phases/04-summary-gen/4-UAT.md
  </read_first>
  <action>
    对改进后的 step7.py 运行 UAT 验证，确保 5 项改进未破坏任何现有功能。

    步骤：
    1. 语法检查: `python3 -c "import py_compile; py_compile.compile('step7.py', doraise=True)"`
    2. CLI 参数测试: `python3 step7.py --date 2026-05-16 --dry-run` — 应正常输出预览
    3. 无 API key 时规则回退: `MINIMAX_API_KEY= python3 step7.py --date 2026-05-16 --dry-run` — 应走 fallback 而非崩溃
    4. 验证输出格式不变（仍包含 ## 栏目 + ### 标题 + 摘要段落）
    5. 如果全部通过，在 4-UAT.md 末尾添加改进验证记录（不改 result 状态，仅记录回归通过）
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path

src = Path('step7.py').read_text()
# Quick syntax
import ast
ast.parse(src)
print('Syntax OK')

# Check UAT still shows 9 pass
uat = Path('.planning/phases/04-summary-gen/4-UAT.md').read_text()
assert 'passed: 9' in uat or 'result: pass' in uat, 'UAT results lost'
print('UAT intact')
"</automated>
  </verify>
  <acceptance_criteria>
    - py_compile 通过
    - --dry-run 正常运行输出预览
    - 无 API key 时不崩溃，走规则回退
    - 输出格式不变
  </acceptance_criteria>
  <done>改进后 step7.py 回归测试通过，无功能破坏</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 重试逻辑 → API | 重试增加 API 调用次数，可能影响 rate limit |
| sleep 间隔 | 增加脚本总执行时间 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | 重试 + sleep 增加 API 调用 | accept | 仅重试 1 次 + 0.5s 间隔，10-16 条总计 < 20s 额外 |
| T-04-04 | Information Disclosure | 截断提示可能泄露标题 | accept | 标题非敏感信息，仅打印到控制台 |
</threat_model>

<verification>
1. step7.py 语法检查通过
2. 5 项改进全部存在（grep 验证）
3. --dry-run 正常运行
4. 无 API key 时走 fallback 不崩溃
5. 输出格式不变
</verification>

<success_criteria>
- step7.py 包含全部 5 项健壮性改进
- 改进后 UAT 回归测试全部通过
- 无新语法错误或运行时崩溃
</success_criteria>

<output>
Create `.planning/phases/04-summary-gen/04-02-SUMMARY.md` when done
</output>
